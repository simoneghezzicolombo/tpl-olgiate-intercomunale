name: RT-021 sharded territorial corridor corpus V3 [retired]

on:
  push:
    branches: [rt021-frozen-territorial-corridor-corpus-v3]
    paths:
      - .github/workflows/phase2-rt021-sharded-corpus-v3.yml
  workflow_dispatch:

concurrency:
  group: rt021-sharded-territorial-corridor-corpus-v3
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Record retirement
        run: |
          echo 'Full-state Yen/K-shortest execution is retired for RT-021.'
          echo 'RT-021 is an RT-006 admitted alternative-pool task, not a complete K-shortest enumeration task.'
          echo 'Use the certified RT-006 bounded + validated sensitivity-grid corpus workflow.'
