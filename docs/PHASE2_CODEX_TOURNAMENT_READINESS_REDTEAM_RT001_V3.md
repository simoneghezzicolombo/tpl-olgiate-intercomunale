# Phase 2 Codex tournament/readiness red-team — RT001 V3

## Scope

This is an independent semantic red-team of Codex's final-tournament readiness, legacy-contract audit and V3 non-decisional Pareto frontier on frozen Codex head `58c228ed614b4fb4d43c15a5c8e12e637074c5f2`.

It does **not** build a competing tournament and does not select a budget, uncertainty band, calendar, recovery, PRIMARY or RUNNER-UP.

## Verdict

The Codex workstream is safe for its declared **non-decisional descriptive** purpose. It fails closed on the two most dangerous missing semantics:

- no candidate-level demand-weighted GJT improvement is manufactured from municipal OD, accessibility or generalized-access screening;
- Stage-E deterministic engineering stress / connection retention is not reinterpreted as an empirical missed-connection probability.

The legacy V2 finalizer remains unauthorized because its schema is not supported by the certified RT001 V3 evidence and its `(scenario_id, plan_id)` identity would collapse 6,961 of 16,495 plan contexts.

## Independent checks

The red-team independently verifies:

- the Codex readiness, contract-audit and frontier validation statuses and lineage hashes;
- all selection, weighting, probability and final-evaluation flags remain false;
- `median_gjt_improvement_min`, `lower_quantile_gjt_improvement_min`, `median_missed_connection_probability`, final `eligible` and integrated `n_sensitivity_runs` remain explicitly unsupported by the legacy-field audit;
- `decision_budget_km` and `uncertainty_band_min` remain caller inputs and are not defaulted;
- forbidden GJT/probability columns do not appear in the context readiness or Pareto frontier outputs;
- all 16,495 contexts remain present in the Pareto membership artifact, including the 4,211 contexts labelled dominated on the reported axes;
- no executable Python consumer of the non-decisional Pareto membership/frontier exists downstream in the audited Codex head;
- no weight is attached to any of the 29 Pareto dimensions.

## Important epistemic limit

The 12,284 non-dominated and 4,211 dominated contexts are statements only about the **reported certified metric vectors inside each budget/evidence-completeness partition**. They are not proof of dominance in the unknown true passenger-utility/reliability system.

This matters especially because the Pareto contract contains three explicitly lower-bound descriptors. Comparing those reported lower bounds is legitimate as descriptive evidence, but it does not resolve their unobserved true values. Therefore:

`nonfrontier_pruning_authorized=false`

`frontier_membership_may_authorize_pruning=false`

All 16,495 contexts must remain available until a separately certified pruning rule or a later fully supported Decision Contract explicitly authorizes elimination.

## Decision boundary

The following remain false:

- `final_tournament_execution_ready`;
- `final_selection_authorized`;
- `decision_budget_selected`;
- `uncertainty_band_selected`;
- `calendar_selected`;
- `recovery_selected`;
- `primary_selection_authorised`;
- `runner_up_selection_authorised`;
- `weighted_composite_score`;
- `manufactured_demand_weighted_gjt`;
- `manufactured_missed_connection_probability`.

The red-team PASS therefore means **the current Codex readiness/frontier implementation respects its non-decisional epistemic boundary**. It does not mean the final tournament is ready to run.
